name: Deploy Pages

on:
  workflow_run:
    workflows: ["Daily Tennis Predictions"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      pages: write
      id-token: write
      contents: read

    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
