name: Daily Tennis Predictions

on:
  workflow_dispatch:
  schedule:
    - cron: "05 04 * * *"

permissions:
  contents: read*  pages: write
  id-token: write

*oncurrency:
  group: daily-tennis-*redictions
  cancel-in-progress: t*ue

jobs:
  build:
    runs-on: ub*ntu-latest

    environment:
     *name: github-pages
      url: ${{ *teps.deployment.outputs.page_url }*

    env:
      ODDS_API_KEY: ${{*secrets.ODDS_API_KEY }}
      RAPI*API_KEY: ${{ secrets.RAPIDAPI_KEY *}
      TENNISAPI_RAPIDAPI_KEY: ${* secrets.TENNISAPI_RAPIDAPI_KEY ||*secrets.RAPIDAPI_KEY }}
      TENN*SAPI_CATEGORY_IDS: "3"
      TENNI*API_PROVIDER_ID: "1"
      SGOAPI:*${{ secrets.SGOAPI }}

    steps:
*     - name: Checkout repository
 *      uses: actions/checkout@v4

 *    - name: Setup Python
        u*es: actions/setup-python@v5
      * with:
          python-version: "*.11"

      - name: Install depend*ncies
        run: |
          pyt*on -m pip install --upgrade pip
  *       if [ -f requirements.txt ];*then pip install -r requirements.t*t; fi

      - name: Syntax check *ennisApi files
        run: |
    *     python -m py_compile tennisap*_client.py results_fetcher.py resu*ts_checker.py odds_api.py

      -*name: Check prediction window
    *   id: prediction_window
        r*n: |
          python - <<'PY'
   *      import os
          from dat*time import datetime, timezone, ti*edelta

          now_utc = dateti*e.now(timezone.utc)
          now_*k = now_utc + timedelta(hours=2)

*         event_name = os.getenv("G*THUB_EVENT_NAME", "")
          if*event_name == "workflow_dispatch":*              should_run = "true"
*         else:
              shoul*_run = "true" if now_sk.hour >= 6 *lse "false"

          print(f"UTC*now: {now_utc.isoformat()}")
     *    print(f"SK/CZ approx now: {now*sk.isoformat()}")
          print(*"should_run={should_run}")

      *   with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as f*
              f.write(f"should_ru*={should_run}\n")
          PY

  *   - name: Prepare directories
   *    if: steps.prediction_window.ou*puts.should_run == 'true'
        *un: |
          mkdir -p public
  *       mkdir -p data
          mkd*r -p data/pick_history
          m*dir -p data/pick_history/all
     *    mkdir -p data/pick_history/top*
          mkdir -p data/results
 *        mkdir -p data/marq_ai

   *  - name: Check API secrets
      * if: steps.prediction_window.outpu*s.should_run == 'true'
        run* |
          python - <<'PY'
     *    import os

          print("OD*S_API_KEY:", "OK" if os.getenv("OD*S_API_KEY") else "MISSING")
      *   print("RAPIDAPI_KEY:", "OK" if *s.getenv("RAPIDAPI_KEY") else "MIS*ING")
          print("TENNISAPI_R*PIDAPI_KEY:", "OK" if os.getenv("T*NNISAPI_RAPIDAPI_KEY") else "MISSING")
          print("SGOAPI:", "OK" if os.getenv("SGOAPI") else "MISSING")
          print("TENNISAPI_CATEGORY_IDS:", os.getenv("TENNISAPI_CATEGORY_IDS", "MISSING"))
          print("TENNISAPI_PROVIDER_ID:", os.getenv("TENNISAPI_PROVIDER_ID", "MISSING"))
          PY

      - name: Run predictions
        if: steps.prediction_window.outputs.should_run == 'true'
        run: |
          python update.py

      - name: Build TOP and ALL pages
        if: steps.prediction_window.outputs.should_run == 'true'
        run: |
          python build_pages.py

      - name: Run results checker
        if: steps.prediction_window.outputs.should_run == 'true'
        run: |
          python results_checker.py

      - name: Build results page
        if: steps.prediction_window.outputs.should_run == 'true'
        run: |
          python rss_results.py

      - name: Ensure results page exists
        if: steps.prediction_window.outputs.should_run == 'true'
        run: |
          mkdir -p public/results
          if [ ! -f public/results/index.html ]; then
            cat > public/results/index.html <<'HTML'
          <!doctype html>
          <html>
          <head>
            <meta charset="utf-8">
            <title>Results</title>
          </head>
          <body>
            <h1>Results</h1>
            <p>No results available yet.</p>
          </body>
          </html>
          HTML
          fi

      - name: Setup Pages
        if: steps.prediction_window.outputs.should_run == 'true'
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        if: steps.prediction_window.outputs.should_run == 'true'
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

      - name: Deploy GitHub Pages
        if: steps.prediction_window.outputs.should_run == 'true'
        id: deployment
        uses: actions/deploy-pages@v4
